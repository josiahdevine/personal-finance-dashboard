// Re-export ErrorBoundary to maintain compatibility with existing imports
// This resolves the issue with imports using './components/ErrorBoundary'
export { default } from './ErrorBoundary/ErrorBoundary';
